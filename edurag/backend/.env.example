# ⚙️ EduRAG Backend - Variables de Entorno
# Copia este archivo a .env y completa con tus valores reales

# =============================================================================
# 🔧 CONFIGURACIÓN DE APLICACIÓN
# =============================================================================
APP_NAME=EduRAG
APP_VERSION=1.0.0
ENVIRONMENT=production
DEBUG=False
LOG_LEVEL=INFO

# =============================================================================
# 🌐 CORS (Cross-Origin Resource Sharing)
# =============================================================================
# ⚠️ IMPORTANTE: Agrega aquí tu URL de Vercel después del deployment
# Formato: URLs separadas por comas, sin espacios
# Ejemplo: http://localhost:5173,https://edu-rag-abc123.vercel.app,https://*.vercel.app
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,https://edu-rag-pc2z.vercel.app,https://*.vercel.app

# =============================================================================
# 🗄️ SUPABASE DATABASE
# =============================================================================
# Obtén estos valores de: https://supabase.com/dashboard
# Tu proyecto → Settings → API
SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=tu-anon-key-aqui
SUPABASE_SERVICE_ROLE_KEY=tu-service-role-key-aqui

# =============================================================================
# 🤖 OPENAI API
# =============================================================================
# Obtén tu API key de: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-tu-api-key-aqui
OPENAI_MODEL=gpt-4o-mini
OPENAI_EMBEDDING_MODEL=text-embedding-3-small

# =============================================================================
# 📚 RAG (Retrieval-Augmented Generation) CONFIGURATION
# =============================================================================
RAG_CHUNK_SIZE=500
RAG_CHUNK_OVERLAP=50
RAG_TOP_K=5
RAG_SIMILARITY_THRESHOLD=0.7

# =============================================================================
# 🖥️ SERVER CONFIGURATION (Solo para desarrollo local)
# =============================================================================
# ⚠️ En producción (Render), estas variables son ignoradas
# Render usa $PORT automáticamente
API_PREFIX=/api
HOST=0.0.0.0
PORT=8000
WORKERS=1

# =============================================================================
# 📝 NOTAS IMPORTANTES
# =============================================================================
# 
# 1. NUNCA subas este archivo a Git si contiene valores reales
# 2. El archivo .env está incluido en .gitignore
# 3. En Render, configura estas variables en: Dashboard → Environment
# 4. En Vercel, solo necesitas VITE_API_URL (no estas variables)
# 5. Para actualizar CORS después de deployment:
#    - Copia tu URL de Vercel
#    - Actualízala en CORS_ORIGINS
#    - Commit y push para redeploy automático
#
# =============================================================================
